# Restaurantal-Thing
Full-stack Food Delivery Application built using React.js (Frontend), Spring Boot (Backend), and MySQL (Database). Features include JWT-based authentication, restaurant browsing, a dynamic cart system, and order management.
